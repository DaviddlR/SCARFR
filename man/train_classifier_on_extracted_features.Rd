% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_representations.R
\name{train_classifier_on_extracted_features}
\alias{train_classifier_on_extracted_features}
\title{Train a classifier on top of the latent representations created by a pretrained model.}
\usage{
train_classifier_on_extracted_features(
  df_train,
  pretrained_model_path,
  pretraining_type,
  label_column,
  num_classes,
  exclude_columns = NULL,
  parsnip_classification_model = NULL,
  classification_model_type = NULL,
  dropout = 0.2,
  doitsmall = FALSE,
  save_path = "classifier"
)
}
\arguments{
\item{df_train}{A \code{data.frame} containing the training samples and labels.}

\item{pretrained_model_path}{\code{String}. Path to the pretrained model for feature extraction (.pt file).}

\item{pretraining_type}{A \code{character} indicating the pretraining objective. Default is \code{"SCARF"}, so that it will use a SCARF pretrained module. Available options are \code{["SCARF"]}.}

\item{label_column}{\code{String}. Name of the column containing the labels. Required if \code{want_labels = TRUE}. Default is \code{NULL}.}

\item{num_classes}{\code{Integer}. Total number of unique classes in the target column.}

\item{exclude_columns}{A \code{string} of columns that the models should ignore (i.e target or ID columns). Default is \code{NULL}.}

\item{parsnip_classification_model}{A \code{parsnip} classification model that has been already created and loaded. Default is \code{NULL}}

\item{classification_model_type}{\code{String}. Type of architecture to train. Currently, \code{"MLP", "Random Forest", "XGB", "KNN", "SVM" and "C50"} are supported. Default is \code{NULL}.}

\item{dropout}{\code{Numeric}. Dropout probability for the classification layers. Default is \code{0.2}.}

\item{doitsmall}{\code{Boolean}. If \code{TRUE}, sub-samples the training set to 1\% for quick experimentation. Default is \code{FALSE}.}

\item{save_path}{\code{String}. Path where the trained classifier bundle (.pt) will be saved. Extension ('.pt') should not be included. Default is \code{"classifier"}, which saves a 'classifier.pt' file in the current directory.}
}
\value{
Invisible \code{NULL}. Saves a serialized classifier bundle containing the network weights, hyperparameters, and the target factor levels to disk.
}
\description{
Train a classifier on top of the latent representations created by a pretrained model.
}
\examples{
\donttest{

if (torch::torch_is_installed()) {
  df_train <- data.frame(
    id = 1:100,
    v1 = rnorm(100),
    v2 = factor(sample(c("A", "B"), 100, replace = TRUE)),
    target = sample(c("Class1", "Class2"), 100, replace = TRUE)
  )

  tmp_pretrained <- tempfile(fileext = ".pt")
  tmp_class <- tempfile()

  fit_extractor(
    dataframe_train = df_train,
    pretraining_type = "SCARF",
    exclude_columns = c("id", "target"),
    n_epochs = 1,
    save_path = tmp_pretrained
  )

  # Train classifier
  train_classifier_on_extracted_features(
    df_train = df_train,
    pretrained_model = tmp_pretrained,
    label_column = "target",
    num_classes = 2,
    exclude_columns = c("id", "target"),
    classification_model_type = "MLP",
    save_path = tmp_class
  )

  if (file.exists(tmp_pretrained)) file.remove(tmp_pretrained)
  if (file.exists(paste0(tmp_class, ".pt"))) file.remove(paste0(tmp_class, ".pt"))
}
}
}
